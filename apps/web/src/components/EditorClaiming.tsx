// Re-export from decomposed module for backward compatibility
import { EditorClaiming, EndorseButton } from './editor-claiming';

export { EditorClaiming, EndorseButton };
export default EditorClaiming;
